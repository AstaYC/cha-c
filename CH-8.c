#include <stdio.h>
#include <stdlib.h>

int main()
{

int  n ;
 printf("veuiller entrer la valeur du nombre : ");
 scanf("%d", &n);

 printf("voila pour la forme octal du nombre %d c'est : %o \n", n , n );

 printf("voila pour la forme decimal du nombre %d c'est : %d \n", n , n );

 printf("voila pour la forme hexadecimal du nombre %d c'est : %x \n", n , n );

return 0;
}
